import 'package:flutter/material.dart';


class ScreeenB extends StatelessWidget {
  const ScreeenB({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Screen B"),),
    );
  }
}
